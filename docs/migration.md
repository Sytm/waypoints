# Migration from v3 to v4

- Language files are now formatted with MiniMessage

## Config changes

- Connected worlds have moved from `general.connectedWorlds` to `pointers.connectedWorlds` and the syntax changed